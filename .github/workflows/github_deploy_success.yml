name: Github Job Deployment Success Metrics
on:
  workflow_dispatch:
jobs:
  ctoai-metrics:
    runs-on: ubuntu-latest
    steps:
      - name: 'Report Deployment Success'
        run: |
          echo "Success Deployment"
         
